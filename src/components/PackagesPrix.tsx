import PrixService from "./boite/PrixService";

const PackagesPrix = () => {
    return (
        <section className="bg-[#0B162C] flex flex-col justify-center items-center w-full">
            <PrixService />
        </section>
    );
}

export default PackagesPrix;
